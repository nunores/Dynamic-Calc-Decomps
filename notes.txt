Console Commands


!baits move1 !after move2 - same as above but after taking max damage from move2 and max damage crit

!moves boost - prints all damage ranges for moves in learnset and obtained tms



Advanced Commands

!ppstall - pairs of mons that are immune to a move and bait another move that the other is immune to, include regenerators that take less than 33%

!stopBadOdds - lists moves from pokemon's learnset that let player mon win the 1v1 if AI mon switches in on them, includes TMs and learnset up to level

!plan -  Either find a mon that can switch in on any move and still win the 1v1 OR
		 Find a mon that can switch in on any move and then baits a move onto another mon that will win the 1v1 after taking the move

!simpleplan - Finds a mon that can win the 1v1 after switching in on lowest damaging move, then finds all the mons that bait that move

!plan move - Either finds a mon that can tank the specified move and win the 1v1 or finds a mon that tanks the specified move and then baits a move that 			   onto something else that would then win the 1v1


bait priority list

// Priority list:
        // 1. Less no of hits to ko
            // only if 2 moves that OHKO
            // 2. Move not affected by a resist berry
            // 3. Priority if outsped and a OHKO
        // 4. Not charging
            // only if 2 moves that OHKO
            // 5. Guaranteed KO
        // 6. Significantly more damage than another move (minimum roll of one move higher than highest roll of another)
        // 7. More accuracy
        // 8. Better effect


fastest killing with prio non resist berry 
fastest killing with prio non chargin



Commands that are available with encounter data loaded


// Get Difficulty Value for a trainer mon

Generate list of all encounters, group by location/enc pool, 
    Fields to add:
        enc_rate - chance of finding within pool
        prob - probability of finding a mon that wins the 1v1 in this pool


get 1v1 outcome for every encounter
    store outcomes in hash

calc probability of finding a mon that wins the 1v1 within each loc assuming you choose the highest prob pool
    probs = []
    for each loc
        pool_prob = 0
        for each pool
            if wins1v1
                pool_prob += enc_rate

        pool_prob = pool_prob / 100
        probs << pool_prob

    prob = probs.max



calc how many 1v1 winners on average a player will find  
    prob_sum = 0
    for each loc
        prob_sum += loc.prob


can do the same but adjust for crits or wins1v1 after taking weakest move


Difficulty = avg_answers_found / total_encounters_available




REQUIRED CONFIGS

level available for every evolution item
level obtained for every TM
allow egg moves






